## Some TypeScript Challenges
This project aims to practice TypeScript, starting with the basics and progressing to more difficult concepts. I'm using `tsc` to compile my codes and generate `.js` files to execute on `cmd`.
